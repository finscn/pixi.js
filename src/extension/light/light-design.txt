WebGLDeferredRenderer：
画正常图片 --》 diffuseTexture
画法线贴图 --》 normalsTexture

调用 LightRenderer.flush
利用light 的shader 对 diffuseTexture 和 normalsTexture 进行混合处理

Light 的 render 目的只是为了把自己放入 LightRenderer.lights

===============

Light <-- DisplayObject
Light.shader =  LightShader

PointLight <-- Light
PointLight.shader =  PointLightShader

AmbientLight <-- Light
AmbientLight.shader =  AmbientLightShader

DirectionLight <-- Light
DirectionLight.shader =  DirectionLightShader


var sun = new PointLight(color, brightness, radius));
sun.postion.x=100;
sun.postion.y=100;


rock.lights = [ sun ];
rock.postion.x=0;
rock.postion.y=0;
rock.renderer ====> LightSpriteRenderer
LightSpriteRenderer.renderer(rock):
{
    rock.diffuseMapping;
    rock.normalMapping;
    //  to draw / flush
}


player.lights = null;
player.postion.x=0;
player.postion.y=0;




